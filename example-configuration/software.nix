# things necessary to do work on the system, outside of ricing
{ config, pkgs, ... }:

{
  environment.systemPackages = 
    [ 
      # packages you want put in the $PATH go here, for instance
    ];
}
